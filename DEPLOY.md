# Deploy Accounts Manager

## Option A — Heroku (recommended)

See README. Use Heroku CLI, add Postgres, set SECRET_KEY and optional MAIL_* config vars, then git push heroku main.

## Option B — Vercel

Limited fit; needs external Postgres. Prefer Heroku, Render, Railway, or Fly.io.
